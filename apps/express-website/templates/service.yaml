apiVersion: v1
kind: Service
metadata:
  name: {{ include "express-website.fullname" . }}
  labels:
    {{- include "express-website.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "express-website.selectorLabels" . | nindent 4 }}
